<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Form</title>
</head>
<body>
    <h1>Contact Us</h1>

    <% if (error) { %>
        <p style="color: red;"><%= error %></p>
    <% } %>

    <form action="/contact" method="POST">
        <label for="name">Name:</label><br>
        <input type="text" id="name" name="name" value="<%= formData ? formData.name : '' %>"><br><br>

        <label for="email">Email:</label><br>
        <input type="email" id="email" name="email" value="<%= formData ? formData.email : '' %>"><br><br>

        <label for="message">Message:</label><br>
        <textarea id="message" name="message"><%= formData ? formData.message : '' %></textarea><br><br>

        <button type="submit">Submit</button>
    </form>
</body>
</html>
